apiVersion: apps/v1
kind: Deployment
metadata:
  name: paymentservice
  namespace: ecommerceapp
  labels:
    app: paymentservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: paymentservice
  template:
    metadata:
      labels:
        app: paymentservice
    spec:
      containers:
        - name: paymentservice
          image: ghcr.io/jakubkopecky-dev/paymentservice:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"

            - name: EnableSwagger
              value: "true"

            - name: Jwt__Key
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: Jwt__Key
            - name: Jwt__Issuer
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: Jwt__Issuer
            - name: Jwt__Audience
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: Jwt__Audience

            - name: MessageBroker__Transport
              value: "AzureServiceBus"
            - name: AzureServiceBus__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: AzureServiceBus__ConnectionString

            - name: Stripe__SecretKey
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: Stripe__SecretKey
            - name: Stripe__WebHookSecret
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: Stripe__WebHookSecret
            - name: Stripe__SuccessUrl
              value: "https://paymentservice/api/success"
            - name: Stripe__CancelUrl
              value: "https://paymentservice/api/cancel"
---
apiVersion: v1
kind: Service
metadata:
  name: paymentservice
  namespace: ecommerce
  labels:
    app: paymentservice
spec:
  selector:
    app: paymentservice
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
